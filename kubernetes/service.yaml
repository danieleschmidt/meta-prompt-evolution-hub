apiVersion: v1
kind: Service
metadata:
  labels:
    app: meta-prompt-evolution
  name: meta-prompt-evolution-service
  namespace: meta-prompt-evolution
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: meta-prompt-evolution
  type: ClusterIP
