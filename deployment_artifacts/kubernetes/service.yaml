apiVersion: v1
kind: Service
metadata:
  name: meta-prompt-evolution-hub-service
  namespace: production
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: meta-prompt-evolution-hub
  type: LoadBalancer
