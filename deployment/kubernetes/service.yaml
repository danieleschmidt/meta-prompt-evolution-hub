apiVersion: v1
kind: Service
metadata:
  labels:
    app: meta-prompt-evolution-hub
  name: meta-prompt-evolution-hub-service
  namespace: production
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: meta-prompt-evolution-hub
  type: ClusterIP
